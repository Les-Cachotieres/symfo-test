{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
                {{ form_start(SingUpForm) }}
                {{ form_row(SingUpForm.Email) }}
                {{ form_row(SingUpForm.Password) }}

                <button type="submit" class="btn btn-primary">Créer</button>
             {{ form_end(SingUpForm) }}
        </div>
    </div>
    
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}