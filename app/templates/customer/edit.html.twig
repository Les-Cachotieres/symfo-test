{% extends 'base.html.twig' %}

{% block title %}Modifier client(e){% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-3 mb-4">Editer un client</h1>

    {{ include('customer/_form.html.twig', {'button_label': 'Modifier'}) }}
     {{ include('customer/_delete_form.html.twig') }}

    <a href="{{ path('customer_index') }}">back to list</a>
</div>
{% endblock %}
